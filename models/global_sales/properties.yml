version: 2


sources:
  - name: global_sales
    schema: global_sales
    tables:
      - name: cities
      - name: retailers
      - name: transactions

models:
  - name: countries_by_total_sales
    config:
      materialized: table

  - name: count_view
    config:
      materialized: view